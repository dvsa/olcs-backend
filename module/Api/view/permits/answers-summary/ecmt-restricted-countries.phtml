<?php

if ($hasRestrictedCountries) {
    echo($this->translate('Yes', 'snapshot'));
} else {
    echo($this->translate('No', 'snapshot'));
}

echo('<br>');

$escapedCountryNames = [];
foreach ($countryNames as $countryName) {
    $escapedCountryNames[] = $this->escapeHtml($this->translate($countryName, 'snapshot'));
}

echo(implode(', ', $escapedCountryNames));
