<?php

$translatedEscapedCountryNames = [];
foreach ($countryNames as $countryName) {
    $translatedEscapedCountryNames[] = $this->escapeHtml(
        $this->translate($countryName, 'snapshot')
    );
}

echo implode(', ', $translatedEscapedCountryNames);


