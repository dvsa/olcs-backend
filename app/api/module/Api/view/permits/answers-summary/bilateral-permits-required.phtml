<?php

$lines = [];
foreach ($rows as $row) {
    $line = sprintf(
        $this->translate($row['key'], 'snapshot'),
        $row['count']
    );

    $lines[] = $this->escapeHtml($line);
}

echo implode('<br/>', $lines);
