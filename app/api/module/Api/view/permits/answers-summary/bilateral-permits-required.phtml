<?php

$lines = [];
foreach ($rows as $row) {
    $line = sprintf(
        $this->translate($row['key']),
        $row['count']
    );

    $lines[] = $this->escapeHtml($line);
}

echo implode('<br/>', $lines);
